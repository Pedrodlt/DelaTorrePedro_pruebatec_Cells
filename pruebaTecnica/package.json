{
  "name": "pruebatecnica",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "comentario mejora": "En la documentación de web dev server para una SPA utilizamos app-index y resolvemos el problema de recargar la pagina",
    "start": "web-dev-server --node-resolve --app-index ./index.html --open --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.4.1",
    "eslint": "^8.56.0"
  },
  "dependencies": {
    "@open-wc/dedupe-mixin": "^1.4.0",
    "@vaadin/router": "^1.7.5",
    "lit": "^3.1.0"
  }
}